/**
 * Effective-pack store types — ext-spec §10.9
 * Re-exported for store consumers that need the store record shape.
 */

export type {
  EffectivePackStoreRecord,
  EffectivePackResolutionResult,
} from '../../types/effective-pack.js';
