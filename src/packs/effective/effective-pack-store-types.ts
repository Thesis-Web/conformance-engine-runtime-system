/**
 * Canonical re-export barrel for effective-pack store types.
 * ext-spec §5.1 — required additive path.
 * This file was previously deleted as STUB-009 but is required by canonical spec.
 * Types live in src/types/effective-pack.ts per the unified type strategy.
 */
export type {
  EffectivePackStoreRecord,
  BaseStandardsModule,
  JurisdictionFamilyConfig,
  TierOverlay,
} from '../../types/effective-pack.js';
